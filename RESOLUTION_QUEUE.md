# RESOLUTION QUEUE

> Generated from the unresolved rows of `PRODUCTION_AUDIT.md` by
> `scripts/generate_resolution_queue.py`. Never hand-maintained: a hand-kept queue
> drifts from the audit exactly the way the summary tables did, and then work gets
> silently dropped. Regenerate after every batch.

**Every unresolved applicable requirement appears here.** Nothing is deferred to
"future work": each row carries a remediation scope naming who can close it and
with what. Difficulty and time cost are never a scope — only the absence of an
external system, device, account or product decision is.

## Scope vocabulary

| Scope | Meaning |
|---|---|
| `IN_REPO_RESOLVABLE` | Closable by writing code, tests or row-specific evidence in this repository. |
| `RUNTIME_VERIFIABLE_NOW` | Needs the app running; an emulator in this environment suffices. |
| `EXTERNAL_BLOCKER` | Needs a system, device, account or decision outside this repository. See `EXTERNAL_LAUNCH_BLOCKERS.md`. |
| `PRODUCT_DECISION_REQUIRED` | Blocked on an owner decision, or on a repository rule that forbids the usual remedy. |
| `TRUE_NA` | Genuinely not applicable. Already `N/A` in the audit; does not appear here. |

## Totals

| Metric | Count |
|---|---|
| Unresolved requirements queued | 462 |
| IN_REPO_RESOLVABLE | 291 |
| RUNTIME_VERIFIABLE_NOW | 33 |
| EXTERNAL_BLOCKER | 87 |
| PRODUCT_DECISION_REQUIRED | 51 |

| Status | IN_REPO | RUNTIME | EXTERNAL | PRODUCT |
|---|---|---|---|---|
| FAIL | 12 | 0 | 2 | 7 |
| PARTIAL | 96 | 3 | 31 | 17 |
| UNVERIFIED | 183 | 30 | 37 | 15 |
| BLOCKED | 0 | 0 | 17 | 12 |

## Queue

Ordered by severity, then status, then section. A row leaves this queue only when
its audit row changes status, so the queue can never claim progress the audit does
not already carry.

| # | ID | Sev | Status | Scope | Requirement | Concrete gap | Verification needed |
|---|---|---|---|---|---|---|---|
| 1 | `MP-41-021` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Battery saver. | The app degrades correctly when battery optimisation is left on, but the actual delay under an aggressive OEM battery saver is unmeasured. | Measure timer drift under battery saver on at least one aggressive OEM device (Xiaomi/Huawei/Samsung) and record it in docs/audit/. |
| 2 | `MP-53-003` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Cloud credentials compromised. | Play App Signing ENROLMENT itself is a Play Console fact the repository cannot prove, and MFA status on the Google/GitHub accounts is likewise external. | Confirm enrolment and MFA once in Play Console and record the date in the table at the top of docs/release/dr_and_key_custody.md. |
| 3 | `MP-59-027` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Battery usage. | Actual battery consumption and timer drift under aggressive OEM battery savers are unmeasured for this build. | Measure on at least one aggressive-OEM physical device and record in docs/audit/ (same as section 41 item 21). |
| 4 | `MP-59-029` | P1 | PARTIAL | `IN_REPO_RESOLVABLE` | Play policy. | CALL_PHONE plus REQUEST_IGNORE_BATTERY_OPTIMIZATIONS are both sensitive declarations that Play reviews case by case; approval is an external decision that cannot be verified from the repository. | Blocked on Play review. The repository-side preparation is unusually thorough; ensure the declarations submitted match store/permissions_declaration_notes.md exactly. |
| 5 | `MP-59-030` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Real device test. | This is emulator evidence. The project's own rules state the Doze race and OEM kill lists cannot be proven off physical hardware. | Execute store/REAL_DEVICE_QA_MATRIX.md on physical devices and record in docs/audit/. Emulator instrumentation now covers the regression surface continuously. |
| 6 | `MP-62-020` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Google Play rules. | Actual Play policy compliance is decided by Google's review, and the sensitive CALL_PHONE and REQUEST_IGNORE_BATTERY_OPTIMIZATIONS declarations are reviewed case by case. | Blocked on external review. Submit to the internal-test track first so any declaration objection surfaces before a production rollout. |
| 7 | `MP-77-001` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Product — Kritik flow'lar tamam | The real dial path has never been exercised end to end on hardware with a live entitlement. | Run the billing checklist on the internal-test track, then the real-device QA matrix. |
| 8 | `MP-77-009` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Security — Critical açık yok | MFA on the Play/GitHub accounts and Play App Signing enrolment cannot be verified from the repository. | Confirm both in the consoles; see EXTERNAL_LAUNCH_BLOCKERS.md. |
| 9 | `MP-77-023` | P1 | PARTIAL | `EXTERNAL_BLOCKER` | Legal/store — Uygun | CALL_PHONE and REQUEST_IGNORE_BATTERY_OPTIMIZATIONS are sensitive declarations reviewed case by case by Google; approval is an external decision. | Submit to the internal-test track first so any declaration objection surfaces before a production rollout. |
| 10 | `MP-54-004` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Checkout success. | No checkout outcome has been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 11 | `MP-54-005` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Checkout cancel. | No checkout outcome has been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 12 | `MP-54-006` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Payment failed. | No checkout outcome has been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 13 | `MP-54-007` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Payment requires authentication. | No checkout outcome has been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 14 | `MP-54-008` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Duplicate submit. | No checkout outcome has been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 15 | `MP-54-018` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Subscription create. | Subscription lifecycle transitions have not been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 16 | `MP-54-019` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Upgrade. | Subscription lifecycle transitions have not been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 17 | `MP-54-020` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Downgrade. | Subscription lifecycle transitions have not been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 18 | `MP-54-021` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Cancel. | Subscription lifecycle transitions have not been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 19 | `MP-54-022` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Pause uygunsa. | Subscription lifecycle transitions have not been observed against this build. | Covered by the billing-checklist run on the internal-test track. |
| 20 | `MP-54-023` | P1 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Trial. | Trial start and trial expiry transitions unverified. | Include explicit trial-start and trial-expiry cases in the billing-checklist run. |
| 21 | `MP-54-024` | P1 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Trial expire. | Trial start and trial expiry transitions unverified. | Include explicit trial-start and trial-expiry cases in the billing-checklist run. |
| 22 | `MP-63-006` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | MFA admin. | MFA status on the two accounts that can publish this app is unknown from here. | Confirm MFA is enabled on both, and prefer a hardware key on the Play publisher account. Compromise of that account is the highest-impact single failure this project has. |
| 23 | `MP-73-010` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Payment integrity hatası varsa. | Payment integrity is architecturally sound but unverified end to end. | This is the one open item on the security gate. Run store/BILLING_RELEASE_CHECKLIST.md against the internal-test track before production rollout. |
| 24 | `MP-74-007` | P1 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Payment sonucu belirsizse. | Payment result clarity is unverified (section 54). | Covered by the billing-checklist run. |
| 25 | `MP-77-013` | P1 | UNVERIFIED | `EXTERNAL_BLOCKER` | Performance — Hedefler içinde | The numeric panic-press-to-dial budget has never been measured on physical hardware, and the project's own rules state the Doze race cannot be proven off-device. | Execute store/REAL_DEVICE_QA_MATRIX.md and record timings in docs/audit/. |
| 26 | `MP-41-017` | P1 | BLOCKED | `EXTERNAL_BLOCKER` | Phone call interruption. | The interaction between an in-progress incoming call and an outgoing emergency dispatch cannot be reproduced without both a live entitlement and real telephony. | BLOCKED on the same external dependency as Batch 1 (Play internal-test account). Add an explicit 'incoming call during armed countdown' case to store/REAL_DEVICE_QA_MATRIX.md and run it on hardware wi |
| 27 | `MP-54-001` | P1 | BLOCKED | `EXTERNAL_BLOCKER` | Pricing doğru. | Price, currency and tax configuration cannot be verified from the repository. | Blocked on Play Console access. Verify against DOC store/BILLING_RELEASE_CHECKLIST.md and record the result. |
| 28 | `MP-54-002` | P1 | BLOCKED | `EXTERNAL_BLOCKER` | Currency doğru. | Price, currency and tax configuration cannot be verified from the repository. | Blocked on Play Console access. Verify against DOC store/BILLING_RELEASE_CHECKLIST.md and record the result. |
| 29 | `MP-54-003` | P1 | BLOCKED | `EXTERNAL_BLOCKER` | Tax doğru sistemi kullanıyor. | Price, currency and tax configuration cannot be verified from the repository. | Blocked on Play Console access. Verify against DOC store/BILLING_RELEASE_CHECKLIST.md and record the result. |
| 30 | `MP-04-001` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Design token sistemi var. | Design tokens are absent as a concept. | Create lib/core/design_tokens.dart as the single source for all scales. |
| 31 | `MP-04-005` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Spacing scale var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 32 | `MP-04-006` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Radius scale var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 33 | `MP-04-007` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Shadow/elevation scale var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 34 | `MP-04-008` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Z-index scale var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 35 | `MP-04-009` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Motion duration token'ları var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 36 | `MP-04-010` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Easing/spring token'ları var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 37 | `MP-04-011` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Breakpoint sistemi var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 38 | `MP-04-012` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Icon size scale var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 39 | `MP-04-013` | P2 | FAIL | `IN_REPO_RESOLVABLE` | Component density sistemi var. | This scale does not exist as a named system. | Define the scale in lib/core/design_tokens.dart and migrate call sites incrementally. |
| 40 | `MP-46-028` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Visual regression. | No image-level visual regression protection. | Accept the repo rule and rely on geometry/layout assertions, or reverse the rule deliberately. Do not add goldens silently. |
| 41 | `MP-46-030` | P2 | FAIL | `EXTERNAL_BLOCKER` | Manual accessibility. | Manual screen-reader verification still missing. | Run one TalkBack pass on device and record it. |
| 42 | `MP-53-012` | P2 | FAIL | `EXTERNAL_BLOCKER` | Status page. | There is no rehearsed disaster procedure of any kind beyond the breach-notification document. | Write one short DR document covering the three real scenarios for this project - lost signing key, compromised Play account, bad release already rolled out - and rehearse the roll-forward once on the  |
| 43 | `MP-65-004` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Support ticket. | Support is an unmanaged email inbox. | Appropriate scale for a solo maintainer, but write down a severity scale and a response expectation - a safety product whose panic button fails needs a defined fast path. |
| 44 | `MP-65-005` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Ticket ownership. | Support is an unmanaged email inbox. | Appropriate scale for a solo maintainer, but write down a severity scale and a response expectation - a safety product whose panic button fails needs a defined fast path. |
| 45 | `MP-65-006` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Severity levels. | Support is an unmanaged email inbox. | Appropriate scale for a solo maintainer, but write down a severity scale and a response expectation - a safety product whose panic button fails needs a defined fast path. |
| 46 | `MP-65-007` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Response expectations. | Support is an unmanaged email inbox. | Appropriate scale for a solo maintainer, but write down a severity scale and a response expectation - a safety product whose panic button fails needs a defined fast path. |
| 47 | `MP-79-012` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Monitoring eklenebilir mi? | This remediation lever is permanently unavailable in its usual form. | The substitute is a stronger pre-release gate, which is exactly where this project already invests. Make that substitution explicit in the postmortem template. |
| 48 | `MP-79-013` | P2 | FAIL | `PRODUCT_DECISION_REQUIRED` | Alert eklenebilir mi? | This remediation lever is permanently unavailable in its usual form. | The substitute is a stronger pre-release gate, which is exactly where this project already invests. Make that substitution explicit in the postmortem template. |
| 49 | `MP-01-008` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Yanlış işlem geri alınabiliyor. | No undo window for destructive local actions; the confirm dialog is the only safety net. | Add a short-lived undo (SnackBarAction) for contact delete. Keep the full data wipe confirm-only - it is intentionally terminal. |
| 50 | `MP-01-027` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Partial success state'i varsa tasarlanmış. | Per-target partial outcome (first contact dialled, second not) collapses into one session result in the timeline. | Record and display per-target dispatch outcome in the safety timeline. |
| 51 | `MP-03-009` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Shadow sistemi tutarlı. | Elevation/shadow is a convention, not a token. | Extract an elevation scale into the design-token file proposed in section 4 and reference it from the component themes. |
| 52 | `MP-03-010` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Elevation mantığı tutarlı. | Elevation/shadow is a convention, not a token. | Extract an elevation scale into the design-token file proposed in section 4 and reference it from the component themes. |
| 53 | `MP-04-003` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Semantic color token'ları var. | Safety-state colours are not semantic tokens. | Add semantic safety-state colour tokens to the token file. |
| 54 | `MP-04-004` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Typography token'ları var. | Typography partially bypasses the theme. | Route the remaining literal TextStyles through TextTheme. |
| 55 | `MP-04-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Component API'ları standardize. | Convention without documentation. | Document the shared-widget contract under docs/design/. |
| 56 | `MP-04-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Button component tek kaynaktan geliyor. | Buttons come from the theme in most places, not all. | Audit for hand-rolled buttons and replace them with themed components. |
| 57 | `MP-05-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Text zoom edildiğinde tasarım bozulmuyor. | Scaling above 200% is bounded rather than supported, and the per-screen reflow matrix behind that bound is device work that has not been recorded. | Run the reflow matrix at 200% on device, record it in docs/audit/, and only then consider raising the clamp. |
| 58 | `MP-07-015` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Orientation change. | Orientation change is suppressed rather than supported. | Portrait lock is defensible for a panic-button product; revisit alongside the adaptive-layout work. |
| 59 | `MP-08-008` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Loading. | Button-level loading state not confirmed. | Cover in the proposed golden-test suite. |
| 60 | `MP-08-035` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Character limit. | The limit is enforced and described but not counted live. | Optional: add a counter to the phone field. The current helper text plus inline validation already communicates the bound. |
| 61 | `MP-09-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Spring değerleri sistematik. | Motion values are consistent by convention, not by token. | Add duration and easing tokens to the proposed lib/core/design_tokens.dart. |
| 62 | `MP-09-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Opacity transition temiz. | Motion values are consistent by convention, not by token. | Add duration and easing tokens to the proposed lib/core/design_tokens.dart. |
| 63 | `MP-11-014` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Notification feedback. | Notification presentation not observed at runtime. | Re-run the walkthrough granting POST_NOTIFICATIONS and capture the check-in and countdown notifications. |
| 64 | `MP-11-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Undo feedback. | No undo feedback because there is no undo. | Ships with the undo remediation in section 1. |
| 65 | `MP-12-020` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Error screen reader'a duyuruluyor. | Non-countdown errors are not explicitly announced. | Wrap the validation and entitlement error surfaces in a live region or use SemanticsService.announce. |
| 66 | `MP-12-030` | P2 | PARTIAL | `RUNTIME_VERIFIABLE_NOW` | Touch target yeterince büyük. | Tap-target sizes are asserted in a widget harness, not measured on device; contrast is still unasserted. | Add contrast assertions; run one TalkBack pass on hardware. |
| 67 | `MP-12-031` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Interactive element'lar birbirine aşırı yakın değil. | Same limits. | As above. |
| 68 | `MP-13-014` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Auto-renewal saklanmıyor. | Subscription, trial and auto-renewal wording exists in source and store docs but was not verified on screen. | Re-run the walkthrough with a RevenueCat sandbox key (--dart-define=REVENUECAT_ANDROID_API_KEY) and capture the paywall, or verify in the Play internal-test track. |
| 69 | `MP-14-019` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Date boundary test edilmiş. | Explicit DST-boundary and leap-year test cases are not called out by name, though the alarm-recovery path they would exercise is covered. | Add named DST-transition and 29-February cases to AlarmClockRecoveryTest.kt. |
| 70 | `MP-14-020` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | DST test edilmiş. | Explicit DST-boundary and leap-year test cases are not called out by name, though the alarm-recovery path they would exercise is covered. | Add named DST-transition and 29-February cases to AlarmClockRecoveryTest.kt. |
| 71 | `MP-14-021` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Leap year test edilmiş. | Explicit DST-boundary and leap-year test cases are not called out by name, though the alarm-recovery path they would exercise is covered. | Add named DST-transition and 29-February cases to AlarmClockRecoveryTest.kt. |
| 72 | `MP-16-019` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Phone input locale'a uygun. | No locale-aware phone formatting or normalisation (e.g. +90 handling) is applied. | A deliberately permissive rule is defensible for an emergency dialler - over-strict parsing could reject a working number. Consider display formatting only, never validation tightening. |
| 73 | `MP-19-001` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 400. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 74 | `MP-19-002` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 401. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 75 | `MP-19-003` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 403. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 76 | `MP-19-004` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 404. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 77 | `MP-19-005` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 409. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 78 | `MP-19-006` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 422. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 79 | `MP-19-008` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 500. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 80 | `MP-19-009` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 502. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 81 | `MP-19-010` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 503. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 82 | `MP-19-011` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | 504. | Status codes are handled as one generic failure class rather than individually. | For a tile CDN this collapse is largely correct; add explicit 429 back-off handling, which is the one code with distinct required behaviour. |
| 83 | `MP-19-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Partial response. | No JSON parsing exists on the network path (tiles are PNG), so schema-mismatch handling is vacuous for the app's own traffic; RevenueCat owns its own parsing. | No action for tiles. If any JSON API is ever added, this section becomes materially applicable. |
| 84 | `MP-19-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Corrupted response. | No JSON parsing exists on the network path (tiles are PNG), so schema-mismatch handling is vacuous for the app's own traffic; RevenueCat owns its own parsing. | No action for tiles. If any JSON API is ever added, this section becomes materially applicable. |
| 85 | `MP-19-018` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Invalid JSON. | No JSON parsing exists on the network path (tiles are PNG), so schema-mismatch handling is vacuous for the app's own traffic; RevenueCat owns its own parsing. | No action for tiles. If any JSON API is ever added, this section becomes materially applicable. |
| 86 | `MP-19-019` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | API schema mismatch. | No JSON parsing exists on the network path (tiles are PNG), so schema-mismatch handling is vacuous for the app's own traffic; RevenueCat owns its own parsing. | No action for tiles. If any JSON API is ever added, this section becomes materially applicable. |
| 87 | `MP-19-025` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Retry exponential backoff kullanıyor. | The tile client relies on RetryClient's default backoff rather than an explicitly configured one. | Configure explicit backoff and jitter on the RetryClient in osm_tile_cache_client.dart. |
| 88 | `MP-19-026` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Retry storm yaratmıyor. | No jitter is applied, so many devices recovering simultaneously could align their retries - a low concern given there is no shared backend to overwhelm. | Add jitter to the offline-queue backoff for completeness. |
| 89 | `MP-22-001` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Server-side authorization. | No server-side authorization exists by design (CLAUDE.md rule 1: no developer backend). Writing BOTH SharedPreferences keys on a rooted device grants unbounded emergency access; allowBackup=false bloc | Accepted with rationale. A server check would make the emergency path network-dependent, which is the failure this product exists to avoid. |
| 90 | `MP-23-010` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Notification preferences. | There is no in-app per-category notification preference - the OS channel settings are the only control. | Android notification channels already give per-category control in system settings; consider deep-linking to them from Settings. |
| 91 | `MP-23-012` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Billing settings. | No in-app billing portal - by design, since Play owns subscription management. | Ensure the paywall links out to the Play subscription settings; verify during the paywall walkthrough proposed in section 13. |
| 92 | `MP-23-015` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Subscription olan kullanıcı account silerken uygun flow görüyor. | Whether the wipe flow warns a subscribed user that their subscription continues was not verified at runtime. | Confirm the wipe confirmation dialog mentions that an active Play subscription must be cancelled separately in Play; add it if absent. |
| 93 | `MP-26-006` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Notification preferences. | No in-app notification preferences surface. | Deep-link from Settings to the system notification channel settings. |
| 94 | `MP-27-023` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Feature flags güvenli. | There is no runtime feature-flag or kill-switch mechanism - shipping a behaviour change requires a Play release. | A remote flag service would violate the no-backend envelope. Accept staged Play rollout as the rollout control (see section 50). |
| 95 | `MP-29-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Data integrity DB seviyesinde mümkün olduğunca korunuyor. | The authoritative store has no DB-level integrity guarantees; integrity is enforced by lib/core/services/contact_service.dart alone. | contact_service.dart already carries 93.08% test coverage (CRITICAL_COVERAGE_PASS), which is the correct compensating control for a store with no constraint engine. Keep that gate. |
| 96 | `MP-32-040` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | SAST. | No dedicated security-focused SAST (e.g. a Dart/Kotlin taint analyser) runs in CI; the analyser and linter are quality tools that catch some security-relevant patterns incidentally. | Add a Kotlin/Android security scanner (for example MobSF static analysis, or detekt with security rules) to the release workflow. Mutation testing on the safety kernel is a stronger-than-typical compe |
| 97 | `MP-32-044` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Infrastructure misconfiguration scan. | There is no cloud infrastructure to misconfigure, so the requirement is largely satisfied by construction; the Play Console configuration itself is outside the repository. | Play Console settings (app signing, data safety answers, target audience) are covered by DOC store/PLAY_CONSOLE_CHECKLIST.md but cannot be verified from the repository - see section 63. |
| 98 | `MP-32-046` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Security logging. | Logging is entirely local and never leaves the device, so there is no central security or audit log an operator can review after an incident. | This is a deliberate KVKK trade-off (no telemetry). The compensating control is the user-triggered local export. Document explicitly in the incident-response runbook that post-incident evidence depend |
| 99 | `MP-32-047` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Audit logging. | Logging is entirely local and never leaves the device, so there is no central security or audit log an operator can review after an incident. | This is a deliberate KVKK trade-off (no telemetry). The compensating control is the user-triggered local export. Document explicitly in the incident-response runbook that post-incident evidence depend |
| 100 | `MP-32-048` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Incident response. | No general (non-breach) incident-response runbook exists - see section 67. | Extend the existing breach procedure into a fuller incident runbook covering a bad Play release and a safety-path regression. |
| 101 | `MP-32-049` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Responsible vulnerability disclosure kanalı. | There is no dedicated security.txt or published responsible-disclosure policy distinguishing security reports from general support. | Publish a short vulnerability-disclosure policy on the existing GitHub Pages site (.gh-pages-publish/) and link it from the store listing. |
| 102 | `MP-33-004` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Secrets secret manager'da. | GitHub Actions secrets are the secret manager; there is no dedicated vault. | Appropriate for a solo-maintainer mobile project. No change recommended. |
| 103 | `MP-42-024` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Dependency quotas. | OSM tile usage policy compliance is implemented but the app's aggregate tile volume at scale has not been estimated. | Estimate tile requests per active user per session and confirm the figure against OSM's bulk-use policy before a large marketing push; consider a paid tile provider if volume grows. |
| 104 | `MP-42-025` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Cloud quotas. | RevenueCat plan limits versus projected active users have not been checked. | Confirm the RevenueCat plan tier covers projected monthly active subscribers before launch. |
| 105 | `MP-44-014` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Searchable. | There is no operator-side search - only the user can retrieve their own log. | Inherent to the no-telemetry design. Ensure the support flow tells users how to export and attach their local log. |
| 106 | `MP-44-035` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Distributed tracing. | No business KPI is observable to the operator. | Play Console install/subscription reporting covers the commercial KPIs; accept that engagement KPIs are unavailable without telemetry. |
| 107 | `MP-45-005` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Correctness objective gerekiyorsa. | No production correctness signal. | Covered by the per-release hardware verification in SLO-2. |
| 108 | `MP-45-011` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Warning ile critical ayrılmış. | No paging tier exists. | Appropriate at this scale. |
| 109 | `MP-46-013` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Payment. | No end-to-end purchase test runs against a Play billing sandbox in CI. | A billing sandbox test cannot run in CI without a Play test account; verify manually against the internal-test track using DOC store/BILLING_RELEASE_CHECKLIST.md and record the result. |
| 110 | `MP-46-031` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Performance regression. | No numeric performance regression threshold is measured or enforced. | Structural contract testing is a reasonable proxy; add a measured budget only alongside the physical-device measurement work in section 41. |
| 111 | `MP-47-002` | P2 | PARTIAL | `RUNTIME_VERIFIABLE_NOW` | Existing user. | Returning-user launch not exercised this session. | Include in the re-run of the smoke script. |
| 112 | `MP-47-025` | P2 | PARTIAL | `RUNTIME_VERIFIABLE_NOW` | App background/foreground. | Not exercised in this session's walkthrough. | Include in the smoke-script re-run. |
| 113 | `MP-49-004` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Staging. | Staging exists as a Play track rather than as repository configuration, so it cannot be verified from the repository. | Record a dated confirmation that the internal-test track is populated with the current candidate. |
| 114 | `MP-49-009` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Payment test/live ayrı. | Whether a RevenueCat sandbox/test project is separated from the live project is a RevenueCat dashboard setting outside the repository. | Confirm in the RevenueCat dashboard that sandbox and production projects are separate, and note it in store/BILLING_RELEASE_CHECKLIST.md. |
| 115 | `MP-49-012` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Staging production'a mümkün olduğunca benziyor. | The verification artefact is deliberately not signed with the release key, so it is not a byte-identical staging replica. | Correct as designed - a smoke build must not be signable as the Play app. No change recommended. |
| 116 | `MP-50-006` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Release notes. | No per-release changelog file is maintained in the repository. | Add a CHANGELOG.md updated as part of the release script, and reuse it for the Play 'what's new' text. |
| 117 | `MP-50-012` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Feature flag. | No runtime feature flags (same finding as section 27 item 23). | Play staged rollout percentage is the available runtime control; treat it as the flag mechanism and document that in the rollout runbook. |
| 118 | `MP-50-013` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Kill switch. | There is no in-app kill switch - a shipped build cannot be remotely disabled, because there is no backend to signal it. | Inherent to the no-backend envelope, and arguably correct for a safety app (a remote kill switch on a panic button is itself a safety risk). Document the halt-rollout procedure as the substitute. |
| 119 | `MP-50-014` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Rollback. | Play does not permit rolling back to a lower versionCode - the only true remedy is halting the rollout and shipping a higher-versioned fix. That procedure has not been rehearsed. | Document the Play reality explicitly (halt + roll-forward, not rollback) and rehearse a roll-forward once on the internal-test track so the timing is known before it is needed. |
| 120 | `MP-50-015` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Rollback gerçekten test edilmiş. | Play does not permit rolling back to a lower versionCode - the only true remedy is halting the rollout and shipping a higher-versioned fix. That procedure has not been rehearsed. | Document the Play reality explicitly (halt + roll-forward, not rollback) and rehearse a roll-forward once on the internal-test track so the timing is known before it is needed. |
| 121 | `MP-50-016` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Previous known-good artifact saklanıyor. | Play does not permit rolling back to a lower versionCode - the only true remedy is halting the rollout and shipping a higher-versioned fix. That procedure has not been rehearsed. | Document the Play reality explicitly (halt + roll-forward, not rollback) and rehearse a roll-forward once on the internal-test track so the timing is known before it is needed. |
| 122 | `MP-50-019` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Metrics otomatik izleniyor. | No one is assigned to watch those metrics on a defined cadence, and no threshold for halting is written down (section 45). | Write explicit halt thresholds (crash-free session rate, ANR rate) and a named owner into the rollout runbook. |
| 123 | `MP-50-020` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Error threshold aşılırsa rollout duruyor. | No one is assigned to watch those metrics on a defined cadence, and no threshold for halting is written down (section 45). | Write explicit halt thresholds (crash-free session rate, ANR rate) and a named owner into the rollout runbook. |
| 124 | `MP-53-006` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Ransomware/hostile deletion riskine göre. | The repository and Play Console account remain the hostile-deletion targets. | Confirm the GitHub repository has an off-platform mirror or that a local clone is retained, and note it in the DR section of docs/release/. |
| 125 | `MP-53-011` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Communication plan. | No proactive user-communication channel exists beyond the store listing and support address. | Acceptable at this scale; revisit if the user base grows. |
| 126 | `MP-53-014` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | DR drill/game day yapılıyor. | No drill has been run. | Rehearse one roll-forward on the internal-test track and record the timing. |
| 127 | `MP-54-029` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Entitlement server-side. | Residual local-tamper risk only. | Accepted with rationale. |
| 128 | `MP-54-032` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Financial events audit log. | The app keeps no local financial audit trail, and the operator has no in-repo record. | RevenueCat's dashboard is the financial audit log; note that dependency explicitly in store/BILLING_RELEASE_CHECKLIST.md. |
| 129 | `MP-59-024` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Accessibility. | No TalkBack pass and no guideline-matcher assertions (section 12). | One TalkBack pass plus meetsGuideline assertions. |
| 130 | `MP-62-014` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Refund terms. | The rendered paywall disclosures were not verified on screen (section 55). | Verify during the billing-checklist run. |
| 131 | `MP-63-004` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | IAM least privilege. | GitHub and Google account-level permission scoping is outside the repository. | Covered by the one-off account-hygiene pass. |
| 132 | `MP-63-005` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Production access minimum. | GitHub and Google account-level permission scoping is outside the repository. | Covered by the one-off account-hygiene pass. |
| 133 | `MP-65-015` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Abuse escalation. | No general abuse or security escalation path is defined (section 32 item 49). | Publish a vulnerability-disclosure address distinct from general support. |
| 134 | `MP-65-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Security escalation. | No general abuse or security escalation path is defined (section 32 item 49). | Publish a vulnerability-disclosure address distinct from general support. |
| 135 | `MP-67-001` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Kim alarm alıyor? | No alerting mechanism exists to notice an incident in the first place (section 45) - discovery depends on Play Console vitals or a user email. | Name Play Console vitals as the alerting source and set a checking cadence after each release. |
| 136 | `MP-67-002` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Kim Incident Commander? | No alerting mechanism exists to notice an incident in the first place (section 45) - discovery depends on Play Console vitals or a user email. | Name Play Console vitals as the alerting source and set a checking cadence after each release. |
| 137 | `MP-67-003` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Kim teknik müdahaleyi yapıyor? | No alerting mechanism exists to notice an incident in the first place (section 45) - discovery depends on Play Console vitals or a user email. | Name Play Console vitals as the alerting source and set a checking cadence after each release. |
| 138 | `MP-67-004` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Kim kullanıcı iletişimini yapıyor? | No alerting mechanism exists to notice an incident in the first place (section 45) - discovery depends on Play Console vitals or a user email. | Name Play Console vitals as the alerting source and set a checking cadence after each release. |
| 139 | `MP-67-009` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Payment nasıl disable edilir? | Not documented as a procedure. | Add to the incident document. |
| 140 | `MP-68-011` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Rollback. | Rollback is documented only as halting; the roll-forward reality of Play is not spelled out (section 50). | Covered by the deployment remediation. |
| 141 | `MP-69-012` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | High DPI. | Only one density class is exercised. | Include a low-DPI device in the real-device matrix. |
| 142 | `MP-69-013` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Low DPI. | Only one density class is exercised. | Include a low-DPI device in the real-device matrix. |
| 143 | `MP-74-005` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | App önemli cihazlarda kullanılamıyorsa. | Tablets and foldables are deliberately unsupported (section 7), and no physical device has been tested for this build. | Phones are the target device class and they work; execute the real-device matrix to close the physical-hardware gap. |
| 144 | `MP-74-006` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Kritik accessibility blocker varsa. | No TalkBack pass has been run, so a blocking screen-reader defect cannot be ruled out. | One TalkBack pass over the primary flow before production rollout. |
| 145 | `MP-75-007` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Performance gate green. | No measured performance gate on physical hardware (section 41). | Add the measured dispatch budget once the real-device pass is executed. |
| 146 | `MP-75-012` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Monitoring active. | There is still no telemetry and no dashboard the project operates; the signal is Google's, not the project's. | Accepted by design. Do not close this by adding an analytics SDK - it would contradict the published KVKK commitment. |
| 147 | `MP-75-013` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Alerts active. | There is still no telemetry and no dashboard the project operates; the signal is Google's, not the project's. | Accepted by design. Do not close this by adding an analytics SDK - it would contradict the published KVKK commitment. |
| 148 | `MP-75-014` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Dashboard active. | There is still no telemetry and no dashboard the project operates; the signal is Google's, not the project's. | Accepted by design. Do not close this by adding an analytics SDK - it would contradict the published KVKK commitment. |
| 149 | `MP-75-015` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Rollback tested. | Roll-forward has never been rehearsed, and Play forbids versionCode rollback. | Rehearse a roll-forward once on the internal-test track (section 50). |
| 150 | `MP-75-016` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Feature flags ready. | No runtime feature flags exist (sections 27 and 50). | Treat the staged rollout percentage as the flag and document it. |
| 151 | `MP-75-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | On-call hazır. | No general incident runbook (section 67). | Write the short incident document proposed in section 67. |
| 152 | `MP-75-018` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Runbook hazır. | No general incident runbook (section 67). | Write the short incident document proposed in section 67. |
| 153 | `MP-77-005` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Accessibility — Belirlenen standardı karşılıyor | No TalkBack pass on a physical device; contrast ratios still unmeasured. | Run one TalkBack pass and record it; add contrast assertions. |
| 154 | `MP-77-015` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | Monitoring — Aktif | There is no telemetry the project operates; the signal is Google's and only exists after a rollout begins. | Accepted by design — do NOT close this by adding an analytics SDK (it would contradict the published KVKK commitment). |
| 155 | `MP-77-016` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Alerting — Aktif | Alerting is a human checking Play Console on a cadence, not an automated page. | Appropriate at this scale; revisit if the product grows an operations team. |
| 156 | `MP-77-020` | P2 | PARTIAL | `EXTERNAL_BLOCKER` | Rollback — Hazır | The roll-forward path has never been rehearsed. | Rehearse once on the internal-test track and record the timing. |
| 157 | `MP-77-021` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Support — Hazır | No severity model or stated response expectation; with no telemetry, diagnosis depends on the user exporting their own local log. | Write the one-page support process referenced in docs/release/observability_and_slo.md section 6. |
| 158 | `MP-77-022` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Incident response — Hazır | Not rehearsed. | Fold the rehearsal into the roll-forward drill. |
| 159 | `MP-78-014` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Security events. | No security-event reporting reaches the operator. | Accept: with no telemetry, security events are only visible if a user reports one. Document this in the incident runbook. |
| 160 | `MP-78-015` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Abuse. | No security-event reporting reaches the operator. | Accept: with no telemetry, security events are only visible if a user reports one. Document this in the incident runbook. |
| 161 | `MP-79-001` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Neden oldu? | No standard postmortem form, and question 3 ('why did monitoring not catch it') has a permanent answer here - there is no monitoring (section 44). | Adopt this section as the template and answer question 3 honestly once, in the incident document, rather than per incident. |
| 162 | `MP-79-002` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Neden test yakalamadı? | No standard postmortem form, and question 3 ('why did monitoring not catch it') has a permanent answer here - there is no monitoring (section 44). | Adopt this section as the template and answer question 3 honestly once, in the incident document, rather than per incident. |
| 163 | `MP-79-003` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Neden monitoring yakalamadı? | No standard postmortem form, and question 3 ('why did monitoring not catch it') has a permanent answer here - there is no monitoring (section 44). | Adopt this section as the template and answer question 3 honestly once, in the incident document, rather than per incident. |
| 164 | `MP-79-004` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Neden kullanıcı yakaladı? | No standard postmortem form, and question 3 ('why did monitoring not catch it') has a permanent answer here - there is no monitoring (section 44). | Adopt this section as the template and answer question 3 honestly once, in the incident document, rather than per incident. |
| 165 | `MP-79-005` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | Aynı bug sınıfı başka nerede olabilir? | No standard postmortem form, and question 3 ('why did monitoring not catch it') has a permanent answer here - there is no monitoring (section 44). | Adopt this section as the template and answer question 3 honestly once, in the incident document, rather than per incident. |
| 166 | `MP-80-001` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | all states | No state matrix is required by the definition of done, and no golden tests enforce it (section 8). | Require a golden test for any new screen. |
| 167 | `MP-80-002` | P2 | PARTIAL | `PRODUCT_DECISION_REQUIRED` | responsive | Responsive coverage is deliberately deferred (section 7). | Blocked on the API 37 adaptive-layout workstream. |
| 168 | `MP-80-003` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | accessibility | Accessibility is not part of the written definition of done, and no guideline matchers are asserted (section 12). | Add meetsGuideline assertions to the widget-test convention and name accessibility in the workflow rule. |
| 169 | `MP-80-009` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | error handling | Schema/migration is not named in the definition of done (section 29). | Add it. |
| 170 | `MP-80-016` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | rollout | Rollout is not named in the definition of done. | Add it to the workflow rule. |
| 171 | `MP-80-017` | P2 | PARTIAL | `IN_REPO_RESOLVABLE` | rollback** | Roll-forward is unrehearsed and rollback is impossible on Play (section 50). | Rehearse once and record the timing. |
| 172 | `MP-01-021` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Session expire olursa kullanıcının yaptığı iş mümkün olduğunca kaybolmuyor. | Whether in-progress input (a half-filled contact form) survives a lock/unlock cycle was not exercised. | Add a widget test that locks and unlocks mid-form and asserts field retention. |
| 173 | `MP-05-025` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Turkish İ/i/ı/I case dönüşümleri test edilmiş. | Turkish I/i/ı/İ casing is not exercised by any test even though the shipped locale is Turkish. | Add a unit test asserting locale-correct casing for user-facing case transformations, and prefer toLowerCase('tr') wherever Turkish text is compared. |
| 174 | `MP-06-014` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Contrast yeterli. | No measured WCAG contrast figures exist for the shipped palette. | Compute contrast ratios for the palette, record them in docs/audit/, and add a unit test asserting >=4.5:1 for body-text pairs. |
| 175 | `MP-06-016` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Color blindness durumları kontrol edilmiş. | Colour-blindness rendering unverified. | Run a deuteranopia/protanopia simulation over the captured screenshots. The icon+label pairing in item 15 already mitigates the main risk. |
| 176 | `MP-07-001` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | 320px gibi dar ekranlar. | 320dp-class narrow screens untested. | Run the primary screens on a 320dp-width emulator; minSdk 29 still permits such devices. |
| 177 | `MP-08-003` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus. | Focus visibility was not exercised - the emulator run used touch only. | Attach a hardware keyboard to the emulator and walk the primary flows, or add a widget test asserting focus decoration. |
| 178 | `MP-08-004` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus-visible. | Focus visibility was not exercised - the emulator run used touch only. | Attach a hardware keyboard to the emulator and walk the primary flows, or add a widget test asserting focus decoration. |
| 179 | `MP-08-015` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Keyboard controlled. | Keyboard-driven operation unverified. | Repeat the primary flow walkthrough with a hardware keyboard attached to the emulator. |
| 180 | `MP-09-020` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Layout animation jank oluşturmuyor. | Layout-animation frame cost is unmeasured. | Capture frame timings on a low-end physical device and record them in docs/audit/. |
| 181 | `MP-09-021` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Scroll-triggered animation takılmıyor. | Scroll-triggered animation cost is unmeasured. | Capture with the device QA pass. |
| 182 | `MP-09-025` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Animasyon düşük güçlü cihazda da düzgün. | Low-end device animation smoothness unverified. | Include a low-end device in the physical QA matrix (docs/qa/REAL_DEVICE_QA_MATRIX equivalent lives at store/REAL_DEVICE_QA_MATRIX.md). |
| 183 | `MP-12-001` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Bütün app mouse'suz kullanılabiliyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 184 | `MP-12-002` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Tab order mantıklı. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 185 | `MP-12-003` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus görünür. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 186 | `MP-12-004` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus modal'ın arkasına kaçmıyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 187 | `MP-12-005` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus trap gereken yerde var. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 188 | `MP-12-006` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Modal kapanınca focus tetikleyene dönüyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 189 | `MP-12-007` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Escape uygun yerlerde çalışıyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 190 | `MP-12-008` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Enter/Space doğru component'ı aktive ediyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 191 | `MP-12-009` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Keyboard shortcut'lar conflict yaratmıyor. | Keyboard operability is entirely unverified. On an Android touch product this is a lower-likelihood path than TalkBack, but it is also the path an external keyboard or switch-access user depends on. | Attach a hardware keyboard to the emulator, walk the consent -> contact -> PIN -> home flow, and record the result; add focus-order widget tests for the consent and contact forms. |
| 192 | `MP-12-023` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Text contrast. | Contrast is unmeasured (same gap as section 6 item 14). | Add textContrastGuideline assertions to the existing widget tests and record measured ratios in docs/audit/. |
| 193 | `MP-12-024` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | UI contrast. | Contrast is unmeasured (same gap as section 6 item 14). | Add textContrastGuideline assertions to the existing widget tests and record measured ratios in docs/audit/. |
| 194 | `MP-12-025` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Focus contrast. | Contrast is unmeasured (same gap as section 6 item 14). | Add textContrastGuideline assertions to the existing widget tests and record measured ratios in docs/audit/. |
| 195 | `MP-16-027` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Unsaved changes gerekiyorsa korunuyor. | Unsaved-changes handling unverified. | Add a widget test that pops the contact editor with dirty fields and asserts the intended behaviour. |
| 196 | `MP-26-008` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Deep link doğru. | The tap-through destination was not observed at runtime. | Verify in the notification-granted re-run proposed above. |
| 197 | `MP-27-010` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Memory leak yok. | No memory or listener-leak profiling was performed in this session. | Run a DevTools memory profile across an arm -> cancel -> arm cycle on a physical device and record it in docs/audit/. |
| 198 | `MP-27-011` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Event listener leak yok. | No memory or listener-leak profiling was performed in this session. | Run a DevTools memory profile across an arm -> cancel -> arm cycle on a physical device and record it in docs/audit/. |
| 199 | `MP-27-012` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Zombie timer yok. | No memory or listener-leak profiling was performed in this session. | Run a DevTools memory profile across an arm -> cancel -> arm cycle on a physical device and record it in docs/audit/. |
| 200 | `MP-31-010` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Metadata/EXIF privacy düşünülmüş. | Whether EXIF metadata is stripped from the locally stored fake-call image was not verified. The image never leaves the device, so the exposure is limited to a local forensic read. | Confirm whether image_picker retains EXIF for the fake-call photo; strip it if so, since the file sits in app storage alongside safety data. |
| 201 | `MP-40-022` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Long tasks azaltılmış. | Memory consumption is unmeasured. | Capture a DevTools memory profile on a physical device across an arm/cancel cycle and record it in docs/audit/. |
| 202 | `MP-40-023` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Memory consumption ölçülüyor. | No low-end physical device result is recorded for this build. | Execute the existing real-device QA matrix on a low-end handset and record it. |
| 203 | `MP-41-001` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Cold startup. | Startup time is architecturally controlled but unmeasured against Android's 2-second feedback threshold. | Measure cold/warm/resume start on a physical device and record it. |
| 204 | `MP-41-002` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Warm startup. | Startup time is architecturally controlled but unmeasured against Android's 2-second feedback threshold. | Measure cold/warm/resume start on a physical device and record it. |
| 205 | `MP-41-003` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Resume. | Startup time is architecturally controlled but unmeasured against Android's 2-second feedback threshold. | Measure cold/warm/resume start on a physical device and record it. |
| 206 | `MP-41-004` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Memory. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 207 | `MP-41-005` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Battery. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 208 | `MP-41-006` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | CPU. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 209 | `MP-41-007` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | GPU. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 210 | `MP-41-008` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Network. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 211 | `MP-41-009` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Frame drops. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 212 | `MP-41-010` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Scroll jank. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 213 | `MP-41-011` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Image memory. | No physical-device measurement exists for this build. | Covered by the single remediation batch: execute the real-device QA matrix and record it in docs/audit/. |
| 214 | `MP-41-018` | P2 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Notification interruption. | Notification interruption during an armed session unverified. | Include in the device QA pass. |
| 215 | `MP-47-017` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Keyboard. | Keyboard and screen-reader passes are missing (same gap as section 12). | One TalkBack pass and one hardware-keyboard pass, recorded in docs/qa/. |
| 216 | `MP-47-018` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Screen reader. | Keyboard and screen-reader passes are missing (same gap as section 12). | One TalkBack pass and one hardware-keyboard pass, recorded in docs/qa/. |
| 217 | `MP-48-006` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Branch protection. | Branch protection and required-review settings cannot be verified from the repository contents. | Confirm in GitHub repository settings that main requires a passing CI check before merge, and record a dated screenshot in docs/qa/. On a single-maintainer project required review is arguably moot, bu |
| 218 | `MP-48-007` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Required review. | Branch protection and required-review settings cannot be verified from the repository contents. | Confirm in GitHub repository settings that main requires a passing CI check before merge, and record a dated screenshot in docs/qa/. On a single-maintainer project required review is arguably moot, bu |
| 219 | `MP-48-008` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | CODEOWNERS gerekiyorsa. | Branch protection and required-review settings cannot be verified from the repository contents. | Confirm in GitHub repository settings that main requires a passing CI check before merge, and record a dated screenshot in docs/qa/. On a single-maintainer project required review is arguably moot, bu |
| 220 | `MP-55-006` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Trial ending açık. | Price, billing period, auto-renewal, trial-ending, cancellation path and upgrade/downgrade timing copy were not observed on screen. | Verify each against the rendered paywall during the billing-checklist run; Play policy requires these disclosures, so this doubles as a store-compliance check. |
| 221 | `MP-55-008` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Cancel sonrası ne olacağı açık. | Price, billing period, auto-renewal, trial-ending, cancellation path and upgrade/downgrade timing copy were not observed on screen. | Verify each against the rendered paywall during the billing-checklist run; Play policy requires these disclosures, so this doubles as a store-compliance check. |
| 222 | `MP-55-009` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Upgrade farkı açık. | Price, billing period, auto-renewal, trial-ending, cancellation path and upgrade/downgrade timing copy were not observed on screen. | Verify each against the rendered paywall during the billing-checklist run; Play policy requires these disclosures, so this doubles as a store-compliance check. |
| 223 | `MP-55-010` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Downgrade timing açık. | Price, billing period, auto-renewal, trial-ending, cancellation path and upgrade/downgrade timing copy were not observed on screen. | Verify each against the rendered paywall during the billing-checklist run; Play policy requires these disclosures, so this doubles as a store-compliance check. |
| 224 | `MP-69-010` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Keyboard. | Keyboard and screen-reader modalities unverified (sections 12 and 47). | One keyboard pass and one TalkBack pass, recorded in docs/qa/. |
| 225 | `MP-69-011` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Screen reader. | Keyboard and screen-reader modalities unverified (sections 12 and 47). | One keyboard pass and one TalkBack pass, recorded in docs/qa/. |
| 226 | `MP-69-016` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Slow CPU. | Low-end CPU and low-memory behaviour unverified. | Include a low-end handset in the real-device matrix. |
| 227 | `MP-69-017` | P2 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | Low memory. | Low-end CPU and low-memory behaviour unverified. | Include a low-end handset in the real-device matrix. |
| 228 | `MP-71-001` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Ne işe yaradığını anlayabiliyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 229 | `MP-71-002` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Nereden başlayacağını biliyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 230 | `MP-71-003` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Ana işi tamamlayabiliyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 231 | `MP-71-004` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Hata yaptığında düzeltebiliyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 232 | `MP-71-005` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Bir şey olduğunda neden olduğunu anlıyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 233 | `MP-71-006` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Yardım istemeden ana özelliği keşfedebiliyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 234 | `MP-71-007` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Kullanıcı beklenmedik yerlerde duruyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 235 | `MP-71-008` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Yanlış CTA'ya basıyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 236 | `MP-71-009` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | Terminolojiyi anlıyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 237 | `MP-71-010` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | “Bu ne yapacak?” diye tereddüt ediyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 238 | `MP-71-011` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | App hızlı hissediyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 239 | `MP-71-012` | P2 | UNVERIFIED | `EXTERNAL_BLOCKER` | App güvenilir hissediyor mu? | The checklist asks for five real users given no help. That has not happened, and for a safety product bought on trust it is a material gap: the failure mode is not 'user is confused', it is 'user beli | Run an unmoderated first-run test with five people who have never seen the app. Focus on one question above all: after onboarding, can they correctly state what will happen when they press the panic b |
| 240 | `MP-07-004` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Tablet portrait. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 241 | `MP-07-005` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Tablet landscape. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 242 | `MP-07-006` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Laptop. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 243 | `MP-07-007` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Desktop. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 244 | `MP-07-008` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Ultra-wide. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 245 | `MP-07-011` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Split-screen. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 246 | `MP-07-014` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Foldable cihaz. | The layout is portrait-phone-only by an explicit product decision recorded in the manifest. | Blocked on the adaptive-layout workstream tied to the Google-imposed API 37 target deadline. This is an external platform schedule, not a code defect. |
| 247 | `MP-47-006` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Paid user. | Paid and expired personas are unverified. | Blocked on a Play internal-test track account and a RevenueCat sandbox key - both external dependencies. Use DOC store/CLOSED_TEST_TESTER_GUIDE.md once available. |
| 248 | `MP-47-007` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Expired subscriber. | Paid and expired personas are unverified. | Blocked on a Play internal-test track account and a RevenueCat sandbox key - both external dependencies. Use DOC store/CLOSED_TEST_TESTER_GUIDE.md once available. |
| 249 | `MP-47-013` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Tablet. | Tablet and desktop form factors are deliberately out of scope for this release. | Blocked on the platform-deadline-driven adaptive-layout workstream. |
| 250 | `MP-47-014` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Desktop. | Tablet and desktop form factors are deliberately out of scope for this release. | Blocked on the platform-deadline-driven adaptive-layout workstream. |
| 251 | `MP-59-018` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Multiple screen sizes. | Multiple screen sizes are deliberately deferred (section 7). | Blocked on the API 37 adaptive-layout workstream. |
| 252 | `MP-59-022` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Foldables. | Foldable and large-screen support deliberately deferred until API 37 is targeted. | Blocked on the same adaptive-layout workstream. This is the single largest scheduled platform debt in the project. |
| 253 | `MP-59-023` | P2 | BLOCKED | `PRODUCT_DECISION_REQUIRED` | Large screens. | Foldable and large-screen support deliberately deferred until API 37 is targeted. | Blocked on the same adaptive-layout workstream. This is the single largest scheduled platform debt in the project. |
| 254 | `MP-76-007` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Crash rate normal. | No rollout has occurred, so no crash-rate data exists for this build. | Blocked until the first staged rollout. Set the halt threshold in advance. |
| 255 | `MP-76-012` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Support tickets anormal artmıyor. | No rollout has occurred, so there is no support-volume baseline. | Blocked until rollout; record the baseline during the first staged rollout. |
| 256 | `MP-77-024` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Canary — Sağlıklı | No rollout has occurred, so no canary signal exists. | Blocked until the first staged rollout begins. |
| 257 | `MP-78-001` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Crash monitoring. | Not launched, so no data exists. | Blocked on launch; assign an owner and a cadence in advance. |
| 258 | `MP-78-002` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Error rate. | Not launched, so no data exists. | Blocked on launch; assign an owner and a cadence in advance. |
| 259 | `MP-78-003` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Latency. | Not launched, so no data exists. | Blocked on launch; assign an owner and a cadence in advance. |
| 260 | `MP-78-005` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | User support. | Not launched. | Blocked on launch. |
| 261 | `MP-78-010` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Payment. | Not launched. | Blocked on launch. |
| 262 | `MP-78-020` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | User feedback. | Not launched, so no feedback exists yet. | Blocked on launch. Include review monitoring in the post-launch watch list - for a trust product, reviews are the highest-signal channel available without telemetry. |
| 263 | `MP-78-021` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | App Store/Play reviews. | Not launched, so no feedback exists yet. | Blocked on launch. Include review monitoring in the post-launch watch list - for a trust product, reviews are the highest-signal channel available without telemetry. |
| 264 | `MP-78-022` | P2 | BLOCKED | `EXTERNAL_BLOCKER` | Top support issues. | Not launched, so no feedback exists yet. | Blocked on launch. Include review monitoring in the post-launch watch list - for a trust product, reviews are the highest-signal channel available without telemetry. |
| 265 | `MP-04-024` | P3 | FAIL | `IN_REPO_RESOLVABLE` | Skeleton standardı var. | No skeleton standard. | Not required at this data volume - a spinner overlay is adequate for local reads. Add skeletons only if list loading becomes perceptible. |
| 266 | `MP-04-027` | P3 | FAIL | `IN_REPO_RESOLVABLE` | Component variation'ları belgelenmiş. | Component variations are undocumented. | Add a short component inventory under docs/design/. |
| 267 | `MP-04-015` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Dark theme tanımlıysa tam. | A maintained light theme ships but is unreachable, so it can silently rot. | Either wire a theme setting or delete lightTheme. Dark-only is a defensible product decision; an unreachable second theme is not. |
| 268 | `MP-14-005` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Currency locale-aware. | The app formats no currency or high-precision numbers of its own, so these rules are largely vacuous today. | No action while pricing is rendered entirely by Play billing; revisit if in-app price strings are ever hand-formatted. |
| 269 | `MP-14-006` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Decimal separators doğru. | The app formats no currency or high-precision numbers of its own, so these rules are largely vacuous today. | No action while pricing is rendered entirely by Play billing; revisit if in-app price strings are ever hand-formatted. |
| 270 | `MP-14-007` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Thousand separators doğru. | The app formats no currency or high-precision numbers of its own, so these rules are largely vacuous today. | No action while pricing is rendered entirely by Play billing; revisit if in-app price strings are ever hand-formatted. |
| 271 | `MP-14-008` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Number formats doğru. | The app formats no currency or high-precision numbers of its own, so these rules are largely vacuous today. | No action while pricing is rendered entirely by Play billing; revisit if in-app price strings are ever hand-formatted. |
| 272 | `MP-16-011` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Autofill çalışıyor. | Autofill hints on the phone field were not verified. | Either add autofillHints: [AutofillHints.telephoneNumber] or accept the system picker as the designated path and note it. |
| 273 | `MP-16-012` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Password manager çalışıyor. | Autofill hints on the phone field were not verified. | Either add autofillHints: [AutofillHints.telephoneNumber] or accept the system picker as the designated path and note it. |
| 274 | `MP-29-006` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Check constraints. | Validation is application-side only. | Low value here because the DB is a local cache with one writer, but a CHECK on is_primary IN (0,1) would be cheap. |
| 275 | `MP-29-011` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Transaction boundaries doğru. | No explicit transaction boundaries are declared for multi-statement operations. | Wrap any future multi-table write in a transaction; none exists today. |
| 276 | `MP-29-012` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Isolation level düşünülmüş. | No explicit transaction boundaries are declared for multi-statement operations. | Wrap any future multi-table write in a transaction; none exists today. |
| 277 | `MP-43-023` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Circuit breaker gerekiyorsa. | No formal circuit breaker (open/half-open state machine) exists. | Not warranted for a single dependency behind a 1.8s timeout; the timeout plus local anchor is the simpler correct design. |
| 278 | `MP-43-027` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Jitter. | No jitter in the local retry schedule. | Add jitter for completeness; impact is minimal because there is no shared backend for synchronised retries to overwhelm. |
| 279 | `MP-64-018` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Budget alert. | No budget alert or anomaly detection is configured by this project. | Set a RevenueCat plan-usage alert if the tier has a request cap; otherwise not warranted. |
| 280 | `MP-64-019` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Anomaly detection. | No budget alert or anomaly detection is configured by this project. | Set a RevenueCat plan-usage alert if the tier has a request cap; otherwise not warranted. |
| 281 | `MP-72-003` | P3 | PARTIAL | `IN_REPO_RESOLVABLE` | Uneven padding. | No systematic per-screen padding review has been done; this row is evidenced only for the onboarding contact step. | Extend the device walkthrough to the remaining screens. |
| 282 | `MP-01-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Başlangıç noktası belli. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 283 | `MP-01-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı ne yapmak istediğini anlayabiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 284 | `MP-01-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Primary CTA açık. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 285 | `MP-01-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Secondary CTA primary CTA ile yarışmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 286 | `MP-01-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Bir sonraki adım tahmin edilebilir. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 287 | `MP-01-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı çıkış yolunu biliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 288 | `MP-01-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı geri dönebiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 289 | `MP-01-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Geri alınamayan işlem açıkça belirtiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 290 | `MP-01-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Destructive action confirmation var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 291 | `MP-01-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok kritik işlemlerde ekstra doğrulama var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 292 | `MP-01-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı yarıda bırakırsa state korunuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 293 | `MP-01-013` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Refresh sonrası mantıklı state korunuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 294 | `MP-01-023` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Tüm flow'ların success state'i var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 295 | `MP-01-024` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Failure state'i var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 296 | `MP-01-025` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Empty state'i var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 297 | `MP-01-026` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Loading state'i var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 298 | `MP-01-029` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Timeout state'i düşünülmüş. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 299 | `MP-02-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Navigation hiyerarşisi açık. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 300 | `MP-02-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Ana bölümler birbirinden net ayrılmış. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 301 | `MP-02-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Aynı şey farklı yerlerde farklı isimlerle geçmiyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 302 | `MP-02-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı nerede olduğunu anlayabiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 303 | `MP-02-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı ana sayfaya ulaşabiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 304 | `MP-02-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Menü sırası mantıklı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 305 | `MP-02-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanım sıklığı yüksek özellikler daha kolay erişiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 306 | `MP-02-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Nadir özellikler UI'ı kalabalıklaştırmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 307 | `MP-02-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Settings kategorileri mantıklı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 308 | `MP-02-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok derin menüler yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 309 | `MP-02-016` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcının mental modeliyle bilgi mimarisi uyuşuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 310 | `MP-03-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Pixel-level alignment düzgün. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 311 | `MP-03-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Horizontal alignment düzgün. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 312 | `MP-03-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Vertical alignment düzgün. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 313 | `MP-03-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Grid sistemi tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 314 | `MP-03-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Spacing sistemi tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 315 | `MP-03-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Rastgele 13px/17px/21px gibi değerler yok; token sistemi kullanılıyor. | No design source of truth (Figma or spec) exists to verify pixel alignment against. | Either publish a design reference or accept visual review as the standard, and archive per-screen screenshots under docs/qa/. |
| 316 | `MP-03-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Component radius'ları tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 317 | `MP-03-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Border kalınlıkları tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 318 | `MP-03-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Layer hierarchy anlaşılır. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 319 | `MP-03-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | UI gereksiz kalabalık değil. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 320 | `MP-03-013` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı nereye bakması gerektiğini anlıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 321 | `MP-03-014` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Visual hierarchy güçlü. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 322 | `MP-03-015` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Primary action görsel olarak primary. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 323 | `MP-03-016` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Dangerous action uygun şekilde ayrıştırılmış. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 324 | `MP-03-017` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Disabled state gerçekten disabled gibi görünüyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 325 | `MP-03-018` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Interactive element interactive gibi görünüyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 326 | `MP-03-019` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Decorative element interactive gibi görünmüyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 327 | `MP-03-020` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Empty whitespace bilinçli kullanılmış. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 328 | `MP-03-021` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Alignment ekranlar arasında değişmiyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 329 | `MP-03-022` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Aynı component farklı ekranlarda farklı davranmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 330 | `MP-03-023` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | İçerik yoğunluğu kullanım senaryosuna uygun. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 331 | `MP-03-024` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Gereksiz modal kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 332 | `MP-03-025` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Gereksiz tooltip kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 333 | `MP-03-026` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | UI jargon kullanmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 334 | `MP-03-027` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Placeholder üretim içeriği kalmamış. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 335 | `MP-03-028` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Lorem ipsum yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 336 | `MP-03-029` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Demo görseller yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 337 | `MP-03-030` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Broken image yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 338 | `MP-03-031` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Missing icon yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 339 | `MP-05-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Font ailesi bilinçli seçilmiş. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 340 | `MP-05-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Fallback font var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 341 | `MP-05-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Heading hierarchy semantik. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 342 | `MP-05-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | H1/H2/H3 görsel olarak ayırt ediliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 343 | `MP-05-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Heading kullanımı yalnızca font büyütme amacıyla yapılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 344 | `MP-05-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Body text rahat okunuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 345 | `MP-05-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Line-height yeterli. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 346 | `MP-05-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Letter-spacing mantıklı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 347 | `MP-05-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok uzun satırlar yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 348 | `MP-05-013` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok kısa satırlar yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 349 | `MP-05-014` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Bold gereksiz kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 350 | `MP-05-015` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Uppercase aşırı kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 351 | `MP-05-016` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Link'ler text'ten ayırt edilebiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 352 | `MP-05-021` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Uzun lokalizasyon metinleri UI'ı bozmuyor. | Long-translation overflow untested because one locale ships. | Becomes relevant when a second locale ships. |
| 353 | `MP-06-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Brand renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 354 | `MP-06-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Semantic success rengi var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 355 | `MP-06-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Warning rengi var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 356 | `MP-06-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Error rengi var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 357 | `MP-06-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Info rengi var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 358 | `MP-06-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Background renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 359 | `MP-06-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Surface seviyeleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 360 | `MP-06-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Border renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 361 | `MP-06-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Hover renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 362 | `MP-06-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Pressed renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 363 | `MP-06-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Selected renkleri tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 364 | `MP-06-013` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Focus indicator rengi tanımlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 365 | `MP-06-018` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | OLED'de aşırı blooming/contrast problemi yok. | OLED behaviour unverified. | Check on a physical OLED device during the existing device QA pass. |
| 366 | `MP-07-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Büyük telefon. | Large-phone form factor untested. | Add to the device QA matrix. |
| 367 | `MP-07-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok yüksek viewport. | Extreme viewport heights untested. | Cover in the device QA matrix. |
| 368 | `MP-07-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Çok kısa viewport. | Extreme viewport heights untested. | Cover in the device QA matrix. |
| 369 | `MP-08-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Read-only. | No read-only state exists to verify. | Not required unless a read-only surface is added. |
| 370 | `MP-08-019` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Tertiary. | No explicit tertiary/ghost button tier. | Define the button tiers when the design-token file is introduced. |
| 371 | `MP-08-020` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Ghost. | No explicit tertiary/ghost button tier. | Define the button tiers when the design-token file is introduced. |
| 372 | `MP-08-031` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Disabled. | No disabled input to verify. | Not required unless one is added. |
| 373 | `MP-08-032` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Read-only. | No read-only input to verify. | Not required unless one is added. |
| 374 | `MP-08-037` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Autofill. | Autofill behaviour unverified. | Verify Android autofill hints on the contact field, or accept that the system contact picker ('Rehberden sec') is the intended fill path. |
| 375 | `MP-09-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Animasyonun amacı var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 376 | `MP-09-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Yalnızca gösteriş için animasyon yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 377 | `MP-09-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Animasyon kullanıcıya state değişimini anlatıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 378 | `MP-09-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Spatial continuity korunuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 379 | `MP-09-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Bir element kaybolup rastgele başka yerde oluşmuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 380 | `MP-09-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Enter animation mantıklı yönden geliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 381 | `MP-09-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Exit animation aynı spatial mantığı takip ediyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 382 | `MP-09-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Gesture ile animation yönü tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 383 | `MP-09-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | UI tepkisi input'tan hemen sonra başlıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 384 | `MP-09-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Animasyon kullanıcıyı bekletmiyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 385 | `MP-09-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Animasyon interrupt edilebiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 386 | `MP-09-013` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Repeated animation rahatsız etmiyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 387 | `MP-09-014` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Duration component boyutuna ve mesafeye uygun. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 388 | `MP-09-015` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Easing tutarlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 389 | `MP-09-018` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Scale transition temiz. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 390 | `MP-09-019` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Blur animation gereksiz kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 391 | `MP-09-023` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Loading animation gereğinden dramatik değil. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 392 | `MP-10-023` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Scroll restoration düzgün. | Scroll restoration across tab switches unverified. | Spot-check during the device QA pass. |
| 393 | `MP-11-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Button press feedback. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 394 | `MP-11-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Toggle feedback. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 395 | `MP-11-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Checkbox feedback. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 396 | `MP-11-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Save feedback. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 397 | `MP-11-012` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Error feedback. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 398 | `MP-12-017` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Heading yapısı doğru. | Screen titles are not marked with Semantics(header: true). | Add header semantics to screen titles - a small, low-risk change that materially improves TalkBack navigation. |
| 399 | `MP-12-029` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | High Contrast/forced colors mümkünse test edilmiş. | High-contrast rendering unverified. | Enable high-contrast text in emulator accessibility settings and re-capture the primary screens. |
| 400 | `MP-13-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | CTA ne olacağını anlatıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 401 | `MP-13-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Blame edici dil yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 402 | `MP-13-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Success message anlamlı. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 403 | `MP-13-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Tooltip kısa. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 404 | `MP-13-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Empty state kullanıcıya ne yapacağını söylüyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 405 | `MP-13-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Confirmation dialog net. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 406 | `MP-13-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Delete confirmation hangi şeyi sileceğini söylüyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 407 | `MP-13-015` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Privacy wording anlaşılır. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 408 | `MP-13-016` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Permission istemeden önce nedeni anlatılıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 409 | `MP-13-018` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı jargon bilmek zorunda değil. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 410 | `MP-14-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Pluralization doğru. | Pluralisation is avoided rather than implemented. | Adopt easy_localization plural() if count-dependent copy is added. |
| 411 | `MP-14-014` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Long translation test edilmiş. | Untestable with one locale. | Revisit when a second locale ships. |
| 412 | `MP-15-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | İlk ekran value proposition'ı gösteriyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 413 | `MP-15-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Zorunlu olmayan onboarding zorunlu yapılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 414 | `MP-15-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı uzun tutorial okumak zorunda değil. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 415 | `MP-15-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Feature gerektiği anda öğretiliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 416 | `MP-15-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Empty state onboarding için kullanılıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 417 | `MP-15-010` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı ilk değer anına hızlı ulaşıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 418 | `MP-15-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Onboarding progress gerekiyorsa gösteriliyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 419 | `MP-16-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Placeholder label yerine kullanılmıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 420 | `MP-16-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Validation anlaşılır. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 421 | `MP-16-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Submit sonrası validation düzgün. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 422 | `MP-16-016` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Enter key mantıklı davranıyor. | Enter/IME action behaviour unverified. | Add a widget test asserting onFieldSubmitted moves focus or submits. |
| 423 | `MP-16-021` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Whitespace normalize ediliyor. | Whitespace handling not explicitly tested. | Add whitespace cases to the emergency_number_validator tests. |
| 424 | `MP-17-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Component loading. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 425 | `MP-17-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Button loading. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 426 | `MP-18-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | İlk defa empty. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 427 | `MP-18-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | User deleted everything. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 428 | `MP-18-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Empty state neden boş olduğunu açıklıyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 429 | `MP-18-008` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Yapılacak sonraki eylem belli. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 430 | `MP-18-009` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Gereksiz illustration asıl mesajın önüne geçmiyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 431 | `MP-20-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Network kaybolunca app çökmez. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 432 | `MP-20-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Kullanıcı connection durumunu anlar. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 433 | `MP-20-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Pending operation belli. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 434 | `MP-20-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Retry var. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 435 | `MP-20-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Duplicate oluşmuyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 436 | `MP-30-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Cache key doğru. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 437 | `MP-30-004` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | TTL doğru. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 438 | `MP-30-005` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Invalidation stratejisi. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 439 | `MP-30-006` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Stale data kabul kriteri. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 440 | `MP-30-007` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Cache stampede önleniyor. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 441 | `MP-33-001` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | API key repository'de yok. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 442 | `MP-33-002` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | .env commit edilmemiş. | Evidence was the section-level assessment verbatim, which does not address this specific requirement (IR-06). | Write row-specific evidence or verify directly; downgraded rather than left as an unsupported PASS. |
| 443 | `MP-41-022` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Device thermal throttling. | Thermal behaviour unverified. | Low priority - the dispatch path is short-lived and not compute-bound. |
| 444 | `MP-47-003` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Power user. | Power-user path unexercised. | Include in the smoke script re-run. |
| 445 | `MP-47-011` | P3 | UNVERIFIED | `IN_REPO_RESOLVABLE` | Account with thousands of objects. | High-volume local data untested. | Low priority given the data model; seed a few hundred timeline events once to confirm scroll behaviour. |
| 446 | `MP-69-014` | P3 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | 60Hz. | Refresh-rate behaviour unmeasured. | Cover during the physical-device performance pass (section 41). |
| 447 | `MP-69-015` | P3 | UNVERIFIED | `RUNTIME_VERIFIABLE_NOW` | High-refresh screen gerekiyorsa. | Refresh-rate behaviour unmeasured. | Cover during the physical-device performance pass (section 41). |
| 448 | `MP-72-001` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Pixel misalignment. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 449 | `MP-72-002` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | 1px border inconsistencies. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 450 | `MP-72-004` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Uneven card height. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 451 | `MP-72-005` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Inconsistent corner radius. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 452 | `MP-72-006` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Text baseline mismatch. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 453 | `MP-72-007` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Icon optical alignment. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 454 | `MP-72-008` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Icon stroke mismatch. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 455 | `MP-72-009` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Incorrect icon weight. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 456 | `MP-72-010` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Button label centering. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 457 | `MP-72-011` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Avatar cropping. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 458 | `MP-72-012` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Image aspect ratio. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 459 | `MP-72-013` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Blurry assets. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 460 | `MP-72-014` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Wrong resolution. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 461 | `MP-72-015` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Shadow clipping. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |
| 462 | `MP-72-016` | P3 | UNVERIFIED | `PRODUCT_DECISION_REQUIRED` | Gradient banding. | Polish is verified by eye on one device at one density, on a subset of screens. Several items in this list (gradient banding, shadow clipping, optical icon alignment) require deliberate close inspecti | Add Flutter golden tests for the highest-traffic screens - they would both establish the current baseline and catch regressions - then do one deliberate polish pass over the captured goldens. |

