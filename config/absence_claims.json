{
  "schemaVersion": 1,
  "note": "Every non-PASS audit row whose evidence asserts an absence is registered here with what would refute it. scripts/verify_absence_claims.py fails if a refuting target exists, or if a registered phrase has drifted out of its row. Written after the final independent review found seven FAIL rows asserting the absence of documents this repository contains (FIR-03). A row may carry MORE THAN ONE claim -- one per canonical cell -- in which case its value is a LIST of field-tagged entries. `field` is \"evidence\" when omitted, which is what the 44 entries written before the gap/remediation scan existed all mean.",
  "claims": {
    "MP-27-023": {
      "claim": "no runtime",
      "refutedBy": [
        "grep:lib:RemoteConfig|FeatureFlagService|FlagStore|firebase_remote_config"
      ],
      "why": "Verified at this revision: lib/ contains no remote-config client, no flag store and no flag service; every gate is keyed on subscription entitlement or device capability, both local facts. flows.py measures this as flagLikeSites = []."
    },
    "MP-32-040": {
      "claim": "no taint-analysis SAST",
      "refutedBy": [
        "grep:.github/workflows:semgrep|github/codeql-action|codeql-analysis"
      ],
      "why": "ci.yml mentions CodeQL only in `sudo rm -rf /opt/hostedtoolcache/CodeQL`, which frees runner disk space and runs no analysis. No dataflow/taint scanner is configured."
    },
    "MP-32-046": {
      "field": "remediation",
      "claim": "no telemetry",
      "refutedBy": [],
      "why": "The absence is real and deliberate: no analytics, no crash SDK and no developer backend ship in this app, so no telemetry channel exists to carry an audit trail off the device. The runbook named in the same cell is cited as ALREADY SATISFIED, not requested -- section 6 documents the local-export dependency, and the cell now says so explicitly. What stays open is the D-6 owner decision, which no repository edit can close."
    },
    "MP-32-047": {
      "field": "remediation",
      "claim": "no telemetry",
      "refutedBy": [],
      "why": "Same cell text and same reasoning as MP-32-046: the telemetry absence is a deliberate KVKK posture, and the incident-runbook reference is a satisfied citation rather than an outstanding request. Registered separately because the two rows are graded independently."
    },
    "MP-41-010": {
      "claim": "no ",
      "refutedBy": [],
      "why": "The claim is about a PHYSICAL panel and GPU, which the repository cannot contain. Emulator measurement is recorded and its limit is stated in the row (E2)."
    },
    "MP-41-011": {
      "claim": "no ",
      "refutedBy": [],
      "why": "The claim is about a PHYSICAL panel and GPU, which the repository cannot contain. Emulator measurement is recorded and its limit is stated in the row (E2)."
    },
    "MP-46-028": {
      "claim": "no golden/screenshot test exists",
      "refutedBy": [
        "grep:test:matchesGoldenFile"
      ],
      "why": "Golden tests are forbidden by .claude/rules/dart/testing.md; no golden invocation exists anywhere under test/."
    },
    "MP-46-030": {
      "claim": "TalkBack",
      "refutedBy": [
        "grep:docs/qa:TalkBack",
        "grep:docs/audit:TalkBack pass"
      ],
      "why": "No TalkBack session is recorded under docs/qa/ or docs/audit/. A physical screen-reader pass is external (E8). The keyboard modality, which the earlier wording also denied, IS recorded and is now graded PASS on MP-47-017 / MP-69-010."
    },
    "MP-47-018": {
      "claim": "TalkBack",
      "refutedBy": [
        "grep:docs/qa:TalkBack",
        "grep:docs/audit:TalkBack pass"
      ],
      "why": "No TalkBack session is recorded under docs/qa/ or docs/audit/. A physical screen-reader pass is external (E8). The keyboard modality, which the earlier wording also denied, IS recorded and is now graded PASS on MP-47-017 / MP-69-010."
    },
    "MP-50-012": {
      "claim": "no runtime",
      "refutedBy": [
        "grep:lib:RemoteConfig|FeatureFlagService|FlagStore|firebase_remote_config"
      ],
      "why": "Verified at this revision: lib/ contains no remote-config client, no flag store and no flag service; every gate is keyed on subscription entitlement or device capability, both local facts. flows.py measures this as flagLikeSites = []."
    },
    "MP-53-012": [
      {
        "claim": "is genuinely absent",
        "refutedBy": [
          "store/status.html",
          "store/durum.html"
        ],
        "why": "No status page exists and none is planned: the product has no server whose state one could report. The DR runbooks the row previously denied DO exist and the row now cites them; only the rollout rehearsal remains, and it needs the Play track (E1/E5).",
        "field": "evidence"
      },
      {
        "field": "gap",
        "claim": "No status page",
        "refutedBy": [],
        "why": "There is no server, no account system and no backend service, so there is no service state a status page could report -- the absence is structural, not an omission. The incident_runbook.md reference in the same cell is a citation of section 8, which records the roll-forward drill as NOT rehearsed; citing an honest negative record is the opposite of a stale claim."
      }
    ],
    "MP-63-005": {
      "claim": "there is no third system to scope",
      "refutedBy": [
        "grep:.github/workflows:DEPLOY_KEY|SSH_PRIVATE_KEY|kubectl|ssh "
      ],
      "why": "There is no server, database host or deploy credential beyond the Play service account; production access is the two consoles, which are outside the repository (E4)."
    },
    "MP-65-004": {
      "claim": "there is no ticketing SYSTEM",
      "refutedBy": [
        "grep:docs/release:ticket system|helpdesk|Zendesk|Freshdesk|Jira Service"
      ],
      "why": "Intake is the support e-mail plus the Play review queue. The channel, the owner, the severity ladder and the response expectation ARE written (incident_runbook.md §1/§2/§6); only the tooling question is open, and it is an owner decision (D-5)."
    },
    "MP-69-011": {
      "claim": "TalkBack",
      "refutedBy": [
        "grep:docs/qa:TalkBack",
        "grep:docs/audit:TalkBack pass"
      ],
      "why": "No TalkBack session is recorded under docs/qa/ or docs/audit/. A physical screen-reader pass is external (E8). The keyboard modality, which the earlier wording also denied, IS recorded and is now graded PASS on MP-47-017 / MP-69-010."
    },
    "MP-71-001": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-002": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-003": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-004": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-005": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-006": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-007": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-008": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-009": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-010": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-011": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-71-012": {
      "claim": "No usability testing with real users is recorded",
      "refutedBy": [
        "grep:docs/qa:usability|kullanilabilirlik testi",
        "grep:docs/research:usability test"
      ],
      "why": "No session with real participants is recorded anywhere. Five unassisted humans cannot be obtained from a repository (E7)."
    },
    "MP-72-002": [
      {
        "field": "remediation",
        "claim": "Include this item in the real-device QA matrix run",
        "refutedBy": [],
        "why": "The matrix is written and this item was measured on the emulator on 2026-08-16 with no defect found; the remainder is how a 1 px border rasterises on a real panel. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
      },
      {
        "field": "evidence",
        "claim": "no defect here at this density",
        "refutedBy": [],
        "why": "The 2026-08-16 emulator pass found no 1 px border inconsistency at 420 dpi. Nothing in the repository can refute an observation about rendered pixels; what would refute it is a real panel showing one, which is why the row stays UNVERIFIED rather than PASS."
      }
    ],
    "MP-72-006": [
      {
        "field": "remediation",
        "claim": "Include this item in the real-device QA matrix run",
        "refutedBy": [],
        "why": "Measured on the emulator 2026-08-16, no defect; sub-pixel baseline drift needs a real panel. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
      },
      {
        "field": "evidence",
        "claim": "no defect here at this density",
        "refutedBy": [],
        "why": "Same pass, baselines aligned in every row inspected. Only a real panel can resolve sub-pixel baseline drift, so the claim is bounded to the density it was measured at."
      }
    ],
    "MP-72-016": [
      {
        "field": "remediation",
        "claim": "Include this item in the real-device QA matrix run",
        "refutedBy": [],
        "why": "Measured with no banding 2026-08-16; banding depends on panel bit depth and dithering, which an emulator framebuffer does not reproduce. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
      },
      {
        "field": "evidence",
        "claim": "no defect here at this density",
        "refutedBy": [],
        "why": "No banding in the two gradient surfaces at 420 dpi. Banding depends on panel bit depth and dithering; an emulator framebuffer cannot exhibit or refute it."
      }
    ],
    "MP-74-007": {
      "claim": "no purchase outcome was observed",
      "refutedBy": [],
      "why": "An indeterminate payment outcome only occurs against real Play billing (E1)."
    },
    "MP-75-016": {
      "claim": "no runtime",
      "refutedBy": [
        "grep:lib:RemoteConfig|FeatureFlagService|FlagStore|firebase_remote_config"
      ],
      "why": "Verified at this revision: lib/ contains no remote-config client, no flag store and no flag service; every gate is keyed on subscription entitlement or device capability, both local facts. flows.py measures this as flagLikeSites = []."
    },
    "MP-77-009": {
      "claim": "No auth/tenant/RCE surface exists",
      "refutedBy": [
        "grep:lib:WebView|webview_flutter|dart:mirrors"
      ],
      "why": "No server, no accounts and no WebView, so the classes of critical finding this row asks about have no surface here. MFA and App Signing state live in the consoles (E4)."
    },
    "MP-77-013": [
      {
        "claim": "no fixed delay, no transition animation, haptics not awaited",
        "refutedBy": [
          "grep:docs/audit:panic-press-to-dial ms|dial gecikmesi ms"
        ],
        "why": "The phrase is a list of properties the structural test ENFORCES, not a claim that a document is missing. What is genuinely absent is a numeric panic-press-to-dial timing from physical hardware, which is device-only by the project rules (E2)."
      },
      {
        "field": "remediation",
        "claim": "Execute store/REAL_DEVICE_QA_MATRIX.md and record timings",
        "refutedBy": [],
        "why": "The matrix is written; the panic-press-to-dial budget must be measured on physical hardware. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
      }
    ],
    "MP-78-001": [
      {
        "claim": "with no SDK required",
        "refutedBy": [],
        "why": "The phrase states that Play Console needs no SDK, not that something is missing. The real gap is post-launch data, which cannot exist before a rollout (E6).",
        "field": "evidence"
      },
      {
        "field": "remediation",
        "claim": "the data does not exist until a rollout does",
        "refutedBy": [],
        "why": "The absent thing is post-launch production data, which cannot exist before a rollout and which no repository edit can create. The observability_and_slo.md reference in the same cell is a satisfied citation -- it already names the owner and the 24h/72h/7-day cadence -- and the cell says so, which is the FIR-03 correction rather than a fresh request."
      }
    ],
    "MP-78-002": [
      {
        "claim": "with no SDK required",
        "refutedBy": [],
        "why": "The phrase states that Play Console needs no SDK, not that something is missing. The real gap is post-launch data, which cannot exist before a rollout (E6).",
        "field": "evidence"
      },
      {
        "field": "remediation",
        "claim": "the data does not exist until a rollout does",
        "refutedBy": [],
        "why": "The absent thing is post-launch production data, which cannot exist before a rollout and which no repository edit can create. The observability_and_slo.md reference in the same cell is a satisfied citation -- it already names the owner and the 24h/72h/7-day cadence -- and the cell says so, which is the FIR-03 correction rather than a fresh request."
      }
    ],
    "MP-78-003": [
      {
        "claim": "with no SDK required",
        "refutedBy": [],
        "why": "The phrase states that Play Console needs no SDK, not that something is missing. The real gap is post-launch data, which cannot exist before a rollout (E6).",
        "field": "evidence"
      },
      {
        "field": "remediation",
        "claim": "the data does not exist until a rollout does",
        "refutedBy": [],
        "why": "The absent thing is post-launch production data, which cannot exist before a rollout and which no repository edit can create. The observability_and_slo.md reference in the same cell is a satisfied citation -- it already names the owner and the 24h/72h/7-day cadence -- and the cell says so, which is the FIR-03 correction rather than a fresh request."
      }
    ],
    "MP-40-023": {
      "field": "remediation",
      "claim": "Execute the existing real-device QA matrix",
      "refutedBy": [],
      "why": "The matrix is written. What is open is EXECUTING it on a low-end handset and recording the memory figure. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-46-013": {
      "field": "remediation",
      "claim": "verify manually against the internal-test track",
      "refutedBy": [],
      "why": "The billing checklist is written. What is open is running it against a Play internal-test account. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-49-009": {
      "field": "remediation",
      "claim": "note it in store/BILLING_RELEASE_CHECKLIST.md",
      "refutedBy": [],
      "why": "The note cannot be written until the RevenueCat dashboard has been inspected, which is the external half. The file exists as the place to record the answer, not as the answer. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-50-014": {
      "field": "remediation",
      "claim": "What remains is the REHEARSAL, which needs the Play internal-test track",
      "refutedBy": [],
      "why": "The runbook half is DONE and the cell now says so (CERT-03). What remains is the rehearsal on the Play internal-test track. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-50-015": {
      "field": "remediation",
      "claim": "What remains is the REHEARSAL, which needs the Play internal-test track",
      "refutedBy": [],
      "why": "Same as MP-50-014: documentation done, rehearsal external. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-50-016": {
      "field": "remediation",
      "claim": "What remains is the REHEARSAL, which needs the Play internal-test track",
      "refutedBy": [],
      "why": "Same as MP-50-014: documentation done, rehearsal external. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-53-003": {
      "field": "remediation",
      "claim": "record the date in the table at the top of docs/release/dr_and_key_custody.md",
      "refutedBy": [],
      "why": "The DR document exists and has the table. The DATE cannot be recorded until Play App Signing enrolment and MFA are confirmed in the console, which is the external half. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-54-001": {
      "field": "remediation",
      "claim": "Verify against DOC store/BILLING_RELEASE_CHECKLIST.md",
      "refutedBy": [],
      "why": "Price, currency and tax live in Play Console. The checklist is the script for verifying them there. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-54-002": {
      "field": "remediation",
      "claim": "Verify against DOC store/BILLING_RELEASE_CHECKLIST.md",
      "refutedBy": [],
      "why": "Same as MP-54-001. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-54-003": {
      "field": "remediation",
      "claim": "Verify against DOC store/BILLING_RELEASE_CHECKLIST.md",
      "refutedBy": [],
      "why": "Same as MP-54-001. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-59-029": {
      "field": "remediation",
      "claim": "ensure the declarations submitted match store/permissions_declaration_notes.md",
      "refutedBy": [],
      "why": "The notes are written. What is open is Google's decision on the submitted declarations, which no repository file can settle. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-59-030": {
      "field": "remediation",
      "claim": "Execute store/REAL_DEVICE_QA_MATRIX.md on physical devices",
      "refutedBy": [],
      "why": "The matrix is written; executing it needs physical hardware. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    },
    "MP-76-012": {
      "field": "remediation",
      "claim": "record the baseline during the first staged rollout",
      "refutedBy": [],
      "why": "There is no support-volume baseline to record until a rollout exists. The runbook is the place it will be recorded, not the missing thing. The named document is the INSTRUCTION SHEET for work that happens outside this repository, not a deliverable this cell is asking someone to write. It exists precisely so the external run has a script to follow; its existence therefore does not make the cell stale. Registered when WORK_PHRASES was widened (CERT-05)."
    }
  }
}
