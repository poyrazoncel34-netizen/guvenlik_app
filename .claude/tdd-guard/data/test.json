{
  "testModules": [
    {
      "moduleId": "test/presentation/providers/settings_provider_theme_test.dart",
      "tests": [
        {
          "name": "themeMode defaults to ThemeMode.system when no prefs saved",
          "fullName": "SettingsProvider themeMode themeMode defaults to ThemeMode.system when no prefs saved",
          "state": "failed",
          "errors": [{"message": "Compilation error: shake_detector_service.dart references sensors_plus which is not in pubspec.yaml (removed in main). Dead shake code must be removed from settings_provider.dart to fix compilation."}]
        }
      ]
    }
  ],
  "reason": "failed"
}
